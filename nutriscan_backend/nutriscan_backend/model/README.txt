Place your trained files here:

1. food_model.keras          ← the model you downloaded from Kaggle
2. class_indices.json        ← the class mapping you saved during training

Without these two files the /predict endpoint will return HTTP 503.
